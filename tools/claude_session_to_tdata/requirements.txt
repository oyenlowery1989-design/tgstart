# Telethon Session to tdata Converter - Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
telethon>=1.34.0
opentele>=1.15.1

# Encryption library (Windows-compatible version)
# If this fails, the script will work without it (just slower)
tgcrypto-pyrofork>=1.2.5

# Alternative converter (uncomment if opentele doesn't work)
# tgconvertor>=0.5.0

# Core cryptographic dependencies (fallback)
pyaes>=1.6.1
rsa>=4.9
pyasn1>=0.4.8
